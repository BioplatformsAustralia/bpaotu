# environment variables for docker-compose.yml
# these are expanded at compose time (not runtime inside a container)

POSTGRES_USER=webapp
POSTGRES_PASSWORD=webapp
POSTGRES_DB=webapp
REDIS_PASSWORD=redispassword
